#!/bin/sh
# initramfs-tools hook: install clawcage-init into the initramfs

PREREQ=""
prereqs() { echo "$PREREQ"; }
case "$1" in prereqs) prereqs; exit 0;; esac

. /usr/share/initramfs-tools/hook-functions

# Copy our custom init (replaces /init in initramfs)
# chmod 755 is critical -- switch_root will exec this
cp /clawcage-init "${DESTDIR}/clawcage-init"
chmod 755 "${DESTDIR}/clawcage-init"
